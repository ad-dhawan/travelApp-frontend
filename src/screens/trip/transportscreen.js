import React from 'react';
import { View, Text } from 'react-native';

const Transport = () => {
    return(
        <>
            <Text>Transport</Text>
        </>
    )
};

export default Transport;